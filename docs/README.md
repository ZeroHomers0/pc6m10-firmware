# 文档目录

项目最新入口仍为根目录 `AGENTS.md` 和 `DOCUMENTATION_INDEX.md`。

- `project/`：当前应用指南、数据段清单和项目状态。
- `analysis/`：按模块整理的逆向结论。
- `w8/`：实机验证总入口、离线证据、硬件清单和软件操作。
- `history/`：历史计划、过程记录和时间点审计，不作为当前状态来源。
- `assets/`：文档图片。

原始硬件资料和反汇编证据不放在文档目录，统一见 `../evidence/README.md`。
