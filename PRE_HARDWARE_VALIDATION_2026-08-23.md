# 实机前验证记录（2026-08-23）

## 结论

当前离线结论为：新增的 `output_stage` 与 `state_machine` 差分矩阵均已通过。仍应遵循本文的
分阶段流程，先做断开功率级的 SWD/供电冒烟和示波器空载验证，不能直接跳到 SCR 带载测试。

## 冻结基线

- Git 标签：`pre-hw-test-01`，指向提交 `95345e4`
- 原始固件 `LPC1765.bin` SHA-256：
  `DD629EAC0243A13CECC826B1FC2B4B54066F7647D8C1D09A62D5CAF7B4F13F65`
- 重编译 `firmware.bin` SHA-256：
  `A3DF49A8D5395E94BDEB771FFE87207017A9AAF0C74B9B0272A81052692CA778`
- 工具链：Arm GNU Toolchain 14.2.Rel1，GCC 14.2.1 20241119

## 新增验证

### 输出级矩阵

`tools/codex_verify_repairs.py` 新增 `output_stage` 原始 BIN/新 ELF A/B 执行级差分：

- 配置：2 种
- 增益选择：3 种
- 控制模式：3 种
- 故障：无故障、bit3、bit4、bit5
- 运行标志：0/1
- 合计：144 例
- 比较内容：SRAM 末态及 FIO/TIMER/SCB 外设写序
- 结果：**144/144 PASS**

### 状态机矩阵

新增主界面、菜单导航、参数编辑等 115 条可终止路径。比较原始 BIN 与新 ELF 的 SRAM 末态；
状态机内部 GPIO 叶函数的逐次 MMIO 由已有独立矩阵负责验证。

初次运行的首个失败用例为：`MENU=1, MENU2=0, MENU3=0, key=0`。

- `0x1000157F`：原始末态 `1`，新 ELF 末态 `0`
- `0x10001581`：原始末态 `1`，新 ELF 末态 `0`

这两个地址位于输入去抖状态区。核对原始反汇编后确认：C 级还原误把复位、急停和 RUN/STOP
扫描包含在 `IDLE>=0x15e` 显示刷新条件中，而原始跳转 `0x4CF2 -> 0x4EBC` 会跳过显示但继续扫描。
调整控制流后，又依次修正 case3/4/5/7 的 `TIMEOUT3` 公共尾部位置。最终结果：
**115/115 PASS**；上述失败均已成为可重复的回归用例。

## 栈与 SRAM 静态检查

使用同一编译选项附加 `-fstack-usage`，共分析 102 个 C 函数：

- 最大单函数静态栈帧：48 B（`closed_loop_integral`、`load_config`）
- `output_stage`：40 B
- `state_machine`：24 B
- `EINT3_IRQHandler`：24 B
- 显示格式化函数最大：40 B

原固件保留的栈/清零区为 `0x1000213C..0x100029C8`，容量 2188 B。单函数栈帧远低于该容量；
但 `-fstack-usage` 不包含完整调用链与中断嵌套，所以只能证明“未见明显单帧溢出”，不能替代实机
MSP 水位监测。首次冒烟时应把该区填入哨兵值，并在最高中断负载后测量最低水位。

## 可重复构建

连续两次完整删除对象并重编译，结果一致：

- `firmware.bin` 两次 SHA-256 均为
  `A3DF49A8D5395E94BDEB771FFE87207017A9AAF0C74B9B0272A81052692CA778`
- `firmware.elf` 两次 SHA-256 均为
  `2F5B025D8F7CD73CE777EA86D6C5CF5E7DCA050EEE787018532373B4D0B80789`

状态机控制流修复后再次连续完整构建两次，当前 `firmware.bin` SHA-256 均为：
`F032EFB70BB3942C4999D7C1F2D0DEBB64125F004C4E19405CAB0DD08F5EAA44`。
前述哈希作为 `pre-hw-test-01` 冻结基线保留，不再代表当前修复产物。

## 实机分阶段验收清单

### 第 0 阶段：上电前

- 核对 MCU 型号、供电电压、SWD 接线和 Boot 引脚。
- 备份原片 Flash、配置字和读保护状态；验证备份可读。
- 功率级、门极驱动和主回路保持断开；使用限流电源。
- 准备一键恢复原始 `LPC1765.bin` 的操作步骤。

### 第 1 阶段：只上控制板

- 先只烧写并复位，确认无 HardFault、无异常复位、SWD 仍可连接。
- 检查 LCD、按键、EEPROM、认证和 RS485，不接触发负载。
- 监测 P0.20/21/22、P1.20..23 上电默认电平，确认继电器不会误吸合。
- 执行 SRAM 栈哨兵检查，记录最小剩余栈空间。

### 第 2 阶段：示波器空载

- 门极驱动输入侧保持与功率器件隔离，先测三相过零输入。
- 对比 TIMER2 相位基准和 6/12 路触发脉冲的顺序、脉宽、间隔及停机撤销时间。
- 分别验证无故障、过压、过流、缺相、急停；任何故障均必须立即撤销触发。

### 第 3 阶段：低压限流负载

- 仅在状态机差分问题关闭、前两阶段全部通过后进行。
- 从隔离低压和最小导通角开始，配置硬件急停、保险和电流限制。
- 同时记录输入过零、门极脉冲、负载电流和故障输出；逐步提高，不跨级测试。

## 放行条件

进入带载测试前必须同时满足：状态机新增矩阵全绿、既有独立验证全绿、栈水位有实测余量、
空载触发时序与原固件示波器结果一致、所有保护路径能可靠撤销触发。
