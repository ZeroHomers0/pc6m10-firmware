# W8 — LPC1765 控制板硬件实测印证流程与软件清单

> 日期：2026-08-22
> 目的：把「目标B 编译产物 → 实板烧写 → 触发脉冲实测 → reg44/45 标定」串成可执行、可判定的流程；
> 明确每个阶段所需硬件、软件及通过标准。
> 关联文档：`docs/LPC1765_PC6M_Debug_Guide.md`（P12 调试接口）、`LPC1769_68_67_66_65_64_63.pdf`（数据手册）、
> `firmware/README.md`（构建）、`docs/HARDWARE_VERIFICATION_2026-08-20.md`（硬件印证）
>
> **前提警示**：当前 `firmware.bin`（63.6KB）为**阶段1-3 可编译骨架**，还在 W1–W7 行为等价路上，
> **还不是**能带动整块 SCR 板的功能性固件。因此本文把 W8 拆成两段：
> - **第Ⅰ段（现在就能做）**：调试/烧写链冒烟——只证明「J-Link↔P12↔LPC1765 能连、能写、能跑任意程序」。
> - **第Ⅱ段（待 W7 出全功能固件后才做）**：整板实测——触发脉冲波形 + reg44/45 标定 + 闭环行为。

---

## 1. W8 目标与完成标准

W8 是目标B 的**最终验收**，用硬件事实回答三件事（全部要可测、可判定）：

| 验收项 | 目标 | 通过标准（可测） |
|---|---|---|
| ① 调试/烧写链 | P12 能连上 LPC1765，能读出芯片，能烧写、能运行 | J-Link `connect` 成功、`device info` 读出 CPU/Flash 型号、可擦/可写/可跑任意小程序 |
| ② 触发脉冲 | 六路 G1-G6 触发脉冲符合逆向结论 | 示波器测得 **12° 脉宽、60° 双脉冲间隔**；触发角 = 180°−当前角；相位排序与 3 相序一致 |
| ③ reg44/45 标定 | Uf/IF 测量值与硬参考一致 | 施加已知参考，Modbus 读 reg45/reg44 与施加值相等（误差在标称量程内），并反推 reg54/reg55 除数在 3500..4500 合法区间 |

**固件现状对应关系**（当前能否做哪段）：

| 固件状态 | 可做 |
|---|---|
| 现 `firmware.bin`（骨架） | 仅第Ⅰ段冒烟 |
| W1–W7 完成后（全功能） | 第Ⅱ段整板实测 |

---

## 2. 关键参数速查（实测用）

### 2.1 触发脉冲（示波器测量对象）

- 主桥 6 路触发 GPIO：**G1–G6 = P0.17 / P0.15 / P0.18 / P2.9 / P2.19 / P2.16**
- 12 脉波扩展：**P2.8 / P2.7 / P2.6 / P2.5 / P0.8 / P0.7**
- 脉宽/间隔：**12° 脉宽、60° 双脉冲**
- 触发角逻辑：**触发角 = 180° − 当前角**（TCK/角度换算见 `docs/PROGRESS_2026-08-20.md` §4f/§4j）
- 生成机制：EINT1/2/3（**P2.11/12/13**，下降沿）每输入交流过零 → 编程 `TIMER2(0x40090000).MR0 = out_freq_adj×10 + BASE + out_fine×K − out_scale` → 匹配 → `TIMER1(0x40008000)` 240 步扫描生成 6 窗口触发脉冲（窗口边界 1160μs、载波 9.26kHz）
- FIO 基址：`0x2009C000`（+0x18 FIO0SET / +0x20 FIO0DIR → 查 P0.x；+0x54 FIO2PIN → 查 P2.x）

**角度→时间换算**（触发脉冲是在过零后的固定相位触发，90°=某时刻）：

| 名称 | 50 Hz（周期 20ms） | 60 Hz（周期 16.667ms） |
|---|---|---|
| 12° 脉宽 | 666.7 μs | 555.6 μs |
| 60° 双脉冲间隔 | 3.333 ms | 2.778 ms |

> 测出值与上表核对；若在 50Hz 设定下测到其它宽度，说明触发常数/频率换算或 MCU 主频相位累加有问题，需回查 `out_freq_adj`/`BASE` 表。

### 2.2 reg44/45 标定（Modbus 实测对象）

| 标号 | RAM 地址 | 物理通道 | 含义 | 换算公式 | 对应标定除数 |
|---|---|---|---|---|---|
| reg44 | `0x10001594` | ADC0 ch3 (AD0.3) | **IF 输出电流反馈** | `(gain_b × avg) / reg54` | reg54 = `0x100016B0` |
| reg45 | `0x10001590` | ADC0 ch4 (AD0.4) | **Uf 输出电压反馈** | `(gain_a × avg) / reg55` | reg55 = `0x100016B8` |
| reg40 | — (ch5 AD0.5) | 给定 Ug | 主显示/闭环参考/远程回显 | `(param4×avg×2)/regxx` | — |
| reg41/42/43 | `0x10001598/9C/A0` | ch2/ch1/ch0 | IA/IB/IC 三相电流 | `(param4×avg×2)/reg51/52/53` | reg51/52/53 |

- reg51–55 范围 **3500..4500**（ADC 各通道满度标定除数，用户可写）。
- **出厂默认**：reg54=reg55=**4095** (=0xFFF=12 位 ADC 满度，`data_image@0x100016B0/0x100016B8`=0x0FFF)。标定就是把 4095 调整为让读数=真实工程量。
- reg44/45 本身**只读**（测量值）；标定 = 写 reg54/55 使读数=施加的真实参考。

### 2.3 调试/烧写接口（P12，来自 Debug_Guide §5.2）

| P12 | 信号 | LPC1765 脚 | 说明 |
|---:|---|---:|---|
| 1 | 3V3 / VTref | 电源 | 调试器参考电平 |
| 2 | GND | 地 | 共地 |
| 3 | P2.10/EINT0/NMI | 53 | ISP 启动控制，板上 R131 上拉 3.3V；**不接 nRESET** |
| 4 | TDO/SWO | 1 | SWD 模式可作 SWO 跟踪 |
| 5 | TDI | 2 | SWD 不用 |
| 6 | TMS/SWDIO | 3 | SWD 双向数据 |
| 7 | TRST# | 4 | SWD 不用 |
| 8 | TCK/SWDCLK | 5 | SWD 时钟 |

- **SWD 四线**：P12 → 1(VTref) / 2(GND) / 6(SWDIO) / 8(SWCLK)。
- **无 RESET#**（MCU 脚 17 未引出）：需要 `Connect under reset` 时应从 MCU 第 17 脚或原板复位网络**另引一根**，不能接 P12-3。
- **无 UART0**（TXD0=98 / RXD0=99）：不能用 USB-TTL 走 UART ISP；若要 ISP 需另引 98/99 脚。
- 参考电平为 **3.3V**，调试信号逻辑电平 **≤3.3V**，不能用 5V/传统 RS232。

---

## 3. 安全与前置条件（上电前必须完成）

1. **断开市电与高压**：P1 交流输入、实际电机/继电器负载、大功率桥一律断开。
2. 等待大电容放电；确认无残余高压。
3. 建立**安全隔离的 3.3V 供电**（独立隔离电源，不要依赖 J-Link 的 VTref 供电）。
4. 万用表**通断档核对**：P12-1↔3.3V、P12-2↔GND、P12-6↔MCU 脚3、P12-8↔MCU 脚5；确认 P12 实物第 1 脚方向（不能凭照片朝向）。
5. 仅用于测量触发逻辑时，用**信号发生器/低压参考**喂同步，不用真市电过零（见 §6.2 步 3）。
6. **记录并备份原始固件**：先 `device info` 读取 CPU/Flash 型号、CRP 保护状态，并**完整 dump 原 Flash 存多份副本**。
   - 严禁 `Unlock` / `Recover` / `Mass Erase`（LPC17xx 有 CRP1/2/3，解除较高保护会整片擦除，原固件不可恢复）。
   - 即使只写部分扇区也可能破坏向量表/配置；**确认可覆盖前不要 loadbin**。
7. TCK/SWDCLK 数据手册注 15：**外部需下拉**。若连接不稳，先降 SWD 速度（100 kHz）再逐步提速，并检查原板是否已有下拉。

---

## 4. 所需硬件清单

| 类别 | 规格 / 型号建议 | 用途 | 是否必需 |
|---|---|---|---|
| **硬件调试器** | **SEGGER J-Link（已确认选用）**，需支持 LPC1765/Cortex-M3、SWD、3.3V 目标电平；无 J-Link 时退而用 CMSIS-DAP / LPC-Link2。避免廉价 ST-Link 克隆 | 第Ⅰ段烧写/调试，第Ⅱ段运行与单步 | **必需** |
| 排针/探针 | 与 P12 对齐的单排 1×8 排针或探针夹具；24cm 杜邦线 4 根 | 连接 P12 | 必需 |
| **示波器** | 建议 ≥100 MHz、≥2 通道、带自动时序测量（脉宽/频率/占空比/相位差）；最好 4 通道（3 相 + 1 同步更省事） | 测 12° 触发脉冲 | 第Ⅱ段必需（第Ⅰ段可选） |
| 无源探头 | 10× 探头 ×2（或 ×4）；测量 MCU GPIO 用 3.3V 兼容探笔 | 探 P0.x/P2.x 触发脚 | 第Ⅱ段必需 |
| 逻辑分析仪（可选） | 8 通道、≥50 MS/s、支持 3.3V | 多路/解帧/低电平逻辑定位优于示波器 | 可选，定位故障利器 |
| **USB-RS485 适配器** | 支持 3.3V TTL/RS485、隔离优先（连接 ADM2483 隔离后端） | Modbus 读 reg40-45 / 写 reg51-55 | 第Ⅱ段必需 |
| 信号发生器 / 低压 AC 源 | 50/60 Hz 可调方波（0~3.3V）或低压交流（如 9V AC 工频），用于合成过零同步 | 脱离市电安全地驱动触发角计算 | 第Ⅱ段必需 |
| 已知参考源 | 可调/已知 DC 电压（Uf 通道）、已知等效电流（IF 通道，可用分压/恒流源模拟） | reg44/45 标定施加真值 | 第Ⅱ段必需 |
| 隔离电源 | 3.3V 隔离供电 + 5V（如需外围） | 保证电脑 USB 地与市电地不构成回路 | 必需 |
| 万用表 | 通断/直流 | P12 方向核对、供电校验 | 必需 |

> 无示波器时的替代：有 MCUXpresso/Keil + 调试器时，可在变量窗口/内存窗口读 `TIMER2.MR0`、`out_freq_adj`、触发 GPIO 寄存器（FIO0SET/FIO0DIR），间接验证触发时序（弱于波形，但可先做）。

---

## 5. 所需软件清单

| 软件 | 版本/用途 | 用于 |
|---|---|---|
| **ARM GNU Toolchain** | `arm-none-eabi-gcc 14.2.Rel1`（`-mcpu=cortex-m3 -mthumb`） | 构建固件（`firmware/build.sh`） |
| **J-Link Commander**（`JLink.exe` / `JLinkExe`） | SEGGER，随 J-Link 驱动安装；CLI 识别/烧写/运行/单步 | **第Ⅰ段主工具**（命令见 §6 第Ⅰ段） |
| **J-Flash / J-Flash Lite** | SEGGER 免费；GUI 或 CLI 烧 `.hex`，带地址/校验管控 | 第Ⅰ段烧 `.hex`（比 Commander 直观） |
| **J-Link GDBServer**（可选） | SEGGER；配合 `arm-none-eabi-gdb` 做源码级单步/读内存 | 第Ⅰ段详调 `TIMER2.MR0` 等内存 |
| OpenOCD + GDB（仅当改换 CMSIS-DAP 才需要） | `openocd -f interface/cmsis-dap.cfg -f target/lpc1765.cfg`；本项目用 J-Link 故**不装** | 备用，非当前路径 |
| **Keil MDK / MCUXpresso / IAR EW**（可选） | 有现成 license 才用；工程需按 LPC1765 256KB 建，不能按 LPC1768/69 的 512KB 建 | GUI 调试/变量观察 |
| **Modbus 主机软件** | `Modbus Poll`（商业）、`QModMaster`、`Simply Modbus`（免费），或 **`pymodbus` + 自写 Python** | 第Ⅱ段读近实时 reg40-45、写 reg51-55 |
| `pymodbus` / `minimalmodbus` | Python 库（串口 RTU master） | 脚本化批量标定/记录曲线 |
| 串口终端/RS485 监视 | 支持 RS485、可设速率；`Simple Serial Port Terminal`、Python `pyserial` | 抓 UART3 原始帧/CRC 校验核对 |
| **示波器软件** | 各家自带（Rigol/Picoscope/Keysight）；带**自动测量**脉宽/频率/相位差 + **截图导出** | 第Ⅱ段抓 12° 脉冲并存证 |
| **逻辑分析仪软件** | PulseView/sigrok（Saleae 兼容），用 PWM/串口解码 | 第Ⅱ段解 6 路触发时序/解 Modbus 帧 |
| Git/`git` | 每次实测后 commit 波形结果、修改的 reg54/55 记录 | 过程留痕 |

**关键软件配置**（第Ⅰ段）：

| 项 | 值 |
|---|---|
| Device | **LPC1765** |
| Interface | **SWD** |
| Speed | **100 kHz 起步**，稳定后→400 kHz→1 MHz |
| Reset strategy | 先 `normal/no hardware reset`；SWD 附着失败再 `Connect under reset`（需另接 RESET 线） |
| 烧写起始地址 | **0x00000000**（.hex 自带地址；.bin 需在工具里手动填 0） |
| 校验 | 烧写后 `verify`，确认 CRC / 向量表 |

**关键软件配置**（第Ⅱ段 Modbus）：

| 项 | 值 |
|---|---|
| 串口参数 | UART3 = RS485；**出厂默认 9600 8N1**（波特率表 `0x100017BC`=2400/4800/**9600**/14400/19200/38400/57600，默认索引 2=9600；校验位默认 0=8N1，CRC16 poly 0xA001） |
| 从机地址 | reg47 = **1**（出厂默认已由固件确证：`data_image@0x10001706`=0x01；live`0x100016FF` 同值。若板上 EEPROM 已改过，先读 reg47 确认） |
| 功能码 | 0x03（读保持）、0x06/0x10（写），范围 reg 1..63 |
| 读测量 | 0x03，起始 reg40，长度 6 → 得 reg40-45 |
| 写标定 | 0x06，reg54/55，值 3500..4500 |

---

## 6. 完整流程

### 第Ⅰ段：调试/烧写链冒烟（现在可做）

> 只证明「能连、能读、能写、能跑」，用最小 blink 程序，**不**触碰 SCR 触发/主整机逻辑。

1. **接线**：J-Link SWD → P12-1/2/6/8；目标板用隔离 3.3V 供电；共地。
2. **识别**（J-Link Commander 交互 CLI：装好驱动后打开 `JLink.exe`，回车后逐行输入）：
   ```
   device LPC1765        ; 选芯片（可直接 "device LPC176x" 再确认）
   si SWD                ; 接口 SWD
   speed 100             ; 先 100 kHz（稳定后再提速）
   connect               ; 连接，输出 Cortex-M3 / Flash 256K / IDCODE
   ```
   连接成功标志：出现 `Found SW-DP`、`IDCode`、`Device core` 信息。
3. **读取状态**：`showdevice` / `mem32 0x400FC41C`（SCB 外设）或 `flash readout`；确认 **CRP 保护状态**。若已保护，**先 dump 备份，不要解锁**。
4. **备份原始 Flash**：`savebin backup_orig.bin, 0x00000000, 0x40000`（LPC1765 Flash = 256 KB = 0x40000）。
5. **写一个最小 blink**（点亮 P0.x / P1.x LED；向量表/SP 正确）：
   ```
   loadbin blink.hex 0x00000000   ; 或 loadbin blink.bin 0x00000000
   r                              ; 复位
   g                              ; 运行（Go）
   ```
   目标板 LED 闪烁 = **烧写/运行链打通**。若复位后死机：`mem32 0x0,4` 看前 4 字（应为 SP/Reset/IRQ），必要时接 RESET 线 + 重启工具勾选 `Connect under reset` 复查向量表。
   （GUI 替代：J-Flash Lite → 选 `NXP LPC1765` + SWD → 拖入 `.hex` → `Program` → `Start`。）
6. **单步/断点**：J-Link 内 `halt`、`setbp 0x<addr>`、`go`，命中后 `st`（单步）/`mem`（读内存）/`regs`（读寄存器），证明调试链路可用。

**第Ⅰ段通过标准**：能 `connect`、能读设备 ID、能备份原 Flash、能烧 blink 且复位后运行、能断点单步。

### 第Ⅱ段A：整板触发脉冲实测（待 W7 全功能固件）

1. **烧录全功能固件**（W7 完成后，行为等价版本）到 `0x00000000`；`verify`。
2. **给安全同步**（关键——脱离市电）：
   - 用信号发生器输出 **50 Hz 0~3.3V 方波** 接 EINT1/2/3 = `P2.11/12/13`（或通过板上分压/同步端子，保证 0-3.3V 逻辑电平、下降沿触发）；
   - 或注入低压工频（如 9V AC）经原同步路径，确认频率满足 `freq_hz>=50` 门控。
   - **负载/大功率桥断开**，只运行控制逻辑。
3. **示波器接触发脚**：
   - 探头1 → G1（P0.17），探头2 → G2（P0.15）（或逐路）；
   - 触发源用 P2.11 同步方波，EXT 或 CH3，上升/下降沿按过零配置。
4. **测量并核对**：
   - 每路脉冲 **12° 脉宽**（50Hz=666.7μs / 60Hz=555.6μs）；
   - **60° 双脉冲间隔**（50Hz=3.333ms / 60Hz=2.778ms）；
   - 6 路相位顺序与三相序一致（A/B/C 相对 G1..G6 落后 120°）；
   - 触发角可变（给定改变 → 触发时刻前移/后移，验证 `180°−当前角`）。
5. **读固件内部核对**（与波形交叉）：断点/变量观察 `TIMER2.MR0`、`out_freq_adj`、`0x10001FFC`(out_setpoint)；或读触发 GPIO 寄存器（FIO0SET/FIO0DIR）。
6. **记录**：截图每路波形（含脉宽/频率/相位差自动测量），存 `W8_trigger/`，commit。

**第Ⅱ段A 通过标准**：6 路均出现 12° 脉宽 + 60° 双脉冲；相位顺序正确；触发角随给定变化且符合 `180°−当前角`；与固件 `TIMER2.MR0` 推算一致。

### 第Ⅱ段B：reg44/45 标定（Modbus）

> 目的：把**物理真值（电压/电流）**与**固件测量显示（reg44/45）**对齐。

1. **连通 RS485**：USB-RS485 → 板上 RS485 端子（A+/B-，ADM2483 隔离后端）；确认从机地址 reg47（默认=1）；用 0x03 读 reg40-45 确认有实时值。

   **Modbus 命令样例**（QModMaster 图形化；或 `pymodbus` 脚本，CRC 自动）：
   ```python
   from pymodbus.client import ModbusSerialClient
   c = ModbusSerialClient(port="COM5", baudrate=9600, parity="N", stopbits=1, bytesize=8, timeout=1)
   c.connect()
   rr = c.read_holding_registers(0x28, 6, slave=1)   # 读 reg40-45（起 0x28=40，长 6）
   vals = rr.registers                                   # [reg40,reg41,reg42,reg43,reg44,reg45]
   c.write_register(0x37, 0x1100, slave=1)               # 写 reg55=0x1100(4352) 标定 Uf
   c.write_register(0x36, 0x1100, slave=1)               # 写 reg54=0x1100(4352) 标定 IF
   c.close()
   ```
   > **本固件 PDU 寄存器地址 = 寄存器号本身**（已由 0x2F→reg47、0x36→reg54、0x37→reg55 实锤）：reg40-45 → 0x28..0x2D（reg44=0x2C、reg45=0x2D）。先读确认从机地址（reg47 已改则 `slave=` 用改后的值）。

2. **施加已知参考**：
   - **Uf（reg45）**：向 ch4（AD0.4）前端注入已知 DC 电压（如 5.00V，经分压/运放链路）；记录施加真值。
   - **IF（reg44）**：向 ch3（AD0.3）注入已知等效电流（或已知分压，模拟经 0.1R 采样 + LM2904 运放 + 增益后的电压）。
3. **读测量**：0x03 读 reg45（Uf）/reg44（IF），记录原始读值。
4. **解算除数**：由 `reg45=Uf = (gain_a × avg)/reg55`、`reg44=IF = (gain_b × avg)/reg54`，反推 reg55/reg54 使读值 = 施加真值；确保结果落在 **3500..4500** 合法区间。
5. **写回**：0x06 写 reg55（再 reg54）；重新读循环，直到 reg45/reg44 与参考差在标称量程内（建议 ≤1% 满度）。
6. **锁定参数**：标定好的 reg54/55 写入后经 `param_sync`(`0x35F2`) 同步到 EEPROM（or 走菜单保存），确认掉电后保留。
7. **交叉验证**：换第二个参考点复测，验证线性；同时在硬件侧用已知电流/录波（电流探头）比对 IF 通道。

**第Ⅱ段B 通过标准**：施加已知 Uf/IF 后，reg45/reg44 读数与其一致（±1% 满度）；反推 reg54/reg55 ∈ [3500,4500]；参数掉电保留。

---

## 7. 常见故障排查

| 现象 | 可能原因 | 处置 |
|---|---|---|
| `J-Link connect` 失败 | P12 方向/线错、无共地、TCK 无下拉、复位锁死 | 核对 P12-1/2/6/8、共地；加 RESET 线 + `Connect under reset`；**降速 100 kHz**；给 TCK 补下拉（数据手册注15） |
| 复位后不运行/死机 | 向量表 SP/Reset 错、链接地址/boot 校验失败 | 检查 `startup.s`/`lpc1765.ld`；确认 `.bin` 基址 0x0、向量表第1字=SP、第2字=Reset；LPC17xx 向量表校验字正确 |
| 能烧不能运行 | 与上同，或烧写地址不对 | 重新指定 0x0 烧写；`verify`；用 `.elf`（带符号）断点定位 |
| 测量无脉冲 | 无过零同步、频率门控 `freq_hz>=50` 不满足、负载断开后固件进安全态 | 确认给到 P2.11/12/13 的有效同步、频率 >=50Hz；看门控条件（`2<=input_locked<=7 && out_param==0`）是否满足 |
| 脉冲时间不对 | 同步频率/相位、主频相位累加、`BASE` 表选错（50/60Hz、mode1/2） | 核对 `freq_hz`、`out_freq_adj`；比对此表 `docs/PROGRESS §4j` 8 组 BASE |
| reg44/45 读不出/恒 0 | RS485 地址/波特率错、ADC 未使能、参考未加 | 核对 reg47 地址与 UART3 速率；确认 AD0CR PDN/通道使能；给到参考信号 |
| reg45 读数差大 | 满度除数未调、前端运放/分压增益偏移 | 用 reg55/reg54 标定；核对链路上运放（LM2904/RS8552XM）是否按设计工作 |
| Modbus 无响应 | A+/B- 接反、无终端电阻、地址错 | 换 A/B、加 120Ω 终端、读 reg47 确认地址 |

---

## 8. 结果记录与归档

- 每次实测后，把以下内容 commit 到本目录：
  - 波形截图 + 自动测量值（脉宽/间隔/相位差）→ `W8_trigger/`
  - 标定前后 reg54/55、reg44/45 值 + 施加参考值表
  - J-Link `device info` 输出、CRP 状态、原 Flash 备份副本
- 用 `git` 记录改动；实测中发现与逆向不符处（相位、脉宽、标定方向）回填到 `HARDWARE_VERIFICATION` / `PROGRESS`。

---

## 9. 一句话结论

**P12 是已确证的 LPC1765 调试口（SWD 四线：P12-1/2/6/8），用一个支持 LPC1765/Cortex-M3 的 J-Link（或 CMSIS-DAP）即可安全地识别、备份、烧写与调试；W8 现可先做调试链冒烟（blink），待 W7 全功能固件后再做整板触发脉冲实测与 reg44/45 标定；全程遵守「断开高压、隔离 3.3V 供电、CRP 未解锁前不擦原固件」三项铁律。**
