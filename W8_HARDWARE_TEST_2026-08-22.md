# W8 硬件实测清单（详细执行版）

> 日期：2026-08-22 ｜ 适用：PC6M-10 三相晶闸管（SCR）移相触发功率控制板 ｜ 面向：完全没做过实测的新手
> 依据：本项目的静态反汇编结论（已验证到 W7）+ `docs/doc/` BOM 硬件文档。
> **读之前一定看第一行安全警告。**

---

# ⚠️⚠️ 安全警告（必须读，必先执行）

这块板子**不是普通单片机板**——它控制的是**三相强电（220V/380V 交流）**，MCU 用晶闸管移相去触发大功率负载（电机、电热、变频前级）。你直接接触主回路引脚会触电、会烧板、会引发火灾。

**绝对红线：**
1. **强电实测（阶段 D）必须由持证电工操作，或至少两人在场，一人操作一人监护。** 我（AI）给的是流程参考，不替代电工安全规程，强电风险自负。
2. **带电时人手不碰任何裸露端子**。用探头前先确认探头耐压、地线接法。
3. **不要用普通单端示波器探头直接测高压侧**（会通过探头地线短路/烧示波器）。高压侧用**差分探头**或**隔离示波器通道**。
4. **永远先测 MCU 引脚（≤3.3V，安全），再考虑高压侧。** 没有过零信号，MCU 不会发触发脉冲（这是本板设计）。
5. 一个实测点做完，**先断电、用万用表确认电容放电（残留高压）**，再改接线。

> 如果不懂「普通探头 vs 差分探头」「隔离通道」「探头地线」区别——**先别做阶段 D**，只做 A/B/C 低压实测，让电工朋友帮你做 D。

---

## 这篇怎么用（三层难度分级）

| 级 | 步骤 | 是否动强电 | 工具 | 安全 |
|---|---|---|---|---|
| **L1 只读** | 阶段 A（上电自检）、阶段 B（Modbus） | **否**（仅控制板 5V/24V 供电） | 万用表、USB-RS485、电脑 | 安全，独自可做 |
| **L2 低压** | 阶段 C（测 MCU 引脚触发脉冲） | 否（只测 ≤3.3V 引脚，用信号发生器**模拟**过零） | 示波器、信号发生器 | 较安全 |
| **L3 强电** | 阶段 D（晶闸管输出闭环） | **是（380V 任意接触）** | 差分探头/隔离示波器 | **必须持证电工** |

**每一步都固定 4 件事：接线 → 测什么 → 预期值 → 判定。** 每步有「记录字段」，看完把结果贴回给我（见我 §6 怎么回传），我帮你判 PASS/FAIL 并定位。

---

## 实测前的准备清单

### 工具
- [ ] 数字万用表（至少测 DC/AC 电压；推荐带真有效值）
- [ ] 示波器（建议 ≥2 通道，50MHz 够；若要抓 3.3ms 间隔的 6 组脉冲，2 通道以上省事）
- [ ] 示波器探头：普通 10:1 探头（测低压）+ **差分探头 或 隔离通道**（强电，阶段 D）
- [ ] 信号发生器（能输出 0~5V 方波，50Hz，用于阶段 C 模拟过零）
- [ ] USB-RS485 转换器（板子 UART3 = RS485，ADM2483 隔离模块）
- [ ] 鳄鱼夹、剥线钳、杜邦线
- [ ] **隔离/独立供电**的控制板电源（见阶段 A1，板子上电方式要按原厂——通常只是控制逻辑板 24V/5V，不接主回路也能上电）

### 软件
- [ ] 串口/Modbus 上位机（串口助手也行；推荐 Python `minimalmodbus`，脚本见 §7 附录）
- [ ] （可选）能存波形文件的示波器

### 关键前提：测试参数预置
实测前先在**面板菜单**把参数调到「保守/可观察」值，避免一上电就动作过猛。推荐预置（地址 = RAM，作用是反汇编确证）：
- 基本屏 15「起始相位」`0x10001660` → 设 **0**（输出下限=180-0=180°，几乎不输出 → 最安全）
- 基本屏 9「相位限制」`0x10001650` → 设 **0**（上限，同上最保守）
- 保护屏：过压/过流阈值调**高**、三相平衡百分比调**大**（先不触发保护，便于观察触发行为）
- 基本屏 11「控制方式」→ 先用**定值**或本地，避免通讯注入干扰

---

# 阶段 A：上电自检（L1，不动主回路，最安全）

> 目的：确认 MCU 在跑、显示正常、电源正常、认证链路正常。**全程不接三相主回路。**

## A1. 给控制板上电（先弄清供电方式）
- **做法**：先只给**控制逻辑**供电。多数此类板 = 内部 DC-DC 把 5V→3.3V（LM2575/AZ1117，见 BOM）；控制板输入端可能是 **24V 直流**或**外置 5V**。**先查 `docs/doc/` 原理图/接线表确认板子输入端子**，别乱接。
- **预期**：LCD 12864 点亮，显示版本/待机画面。
- **判定**：
  - ✅ 点亮且有文字 → MCU 和显示 OK，进 A2
  - ❌ 全黑 → MCU 没跑或晶振没振 / 3.3V 没正常 → 先修电源，再测 A2
- **记录**：实测供电电压、LCD 是否亮、显示内容。

## A2. 测电源轨 + 晶振
- **方法**（万用表，LCD 亮后）：测以下点对 GND 电压。
  - 3.3V 轨（AZ1117 输出，LPC1765 VDD）≈ **3.3V**
  - 5V 轨（LM2575 输出）≈ **5.0V**
  - 12MHz 晶振：示波器 10:1 探头测晶振 **XL1 引脚**，应看到 ≈**12MHz 正弦/方波**，幅度约 1V~3.3V 峰峰。
- **判定**：3.3V 偏差 >±5% 或晶振停振 → MCU 无法正常运行，先解决再继续。
- **记录**：3.3V/5V 实测值、晶振频率示数（如 `11.98MHz`）。

## A3. 确认 MCU 主循环 / 扫描时序（关键泵）
- **方法**（示波器）：测 **TIMER1 扫描**——LCD 的 E 使能引脚（CPUD 文档：`P1.28 = E`，见交叉引用 ⚠ 待实测；实体引脚以板为准）。或直接测某个被反复翻转的 GPIO。
- **预期**：看到一个**周期性**的脉冲串（这是 MCU 在跑 TIMER1/主循环扫 LCD）。**只要有稳定周期脉冲就说明固件活着。**
- **判定**：❌ 无任何稳定周期性信号 → MCU 死循环/晶振问题（回查 A1/A2）。
- **记录**：用示波器测到的周期、频率。截图更好。

## A4. 上电认证挑战-应答（1-Wire 隔离链）——重要！这是锁机源头
- **原理**：固件上电对 ADuM1201 隔离的 1-Wire 外部模块做 24 位挑战/16 位应答，**失败重试 5 次后锁机**（P2.1/2.2/2.3/2.4 = 出/入/时钟/复位，P0.22 运行继电器不吸合、P1.23 故障 LED）。
- **A4a（预测锁机）**：**断开**隔离侧外部认证模块（拔掉排针 DC3 或短路输入的排线）→ 上电。
  - **预期**：等待重试 5 次后（约数秒）→ **锁机**：P1.23 故障 LED 亮 / 运行继电器 RLY1 不吸合 / 报"认证失败"。
  - **判定**：✅ 确实锁机 = 认证逻辑对；❌ 没锁 = 认证模块可能始终在位、或 P2.4 复位逻辑相反（记录！）
- **A4b（预测恢复）**：**接回**隔离侧模块 → 重新上电。
  - **预期**：挑战-应答通过 → 正常进入运行待机，**不锁机**。
  - **判定**：✅ 通过 = 认证链路 OK；❌ 仍锁机 = ADuM1201 供电异常 / 排线松动 / 应答算法不匹配（记录！这是绝佳逆向反馈）。
- **记录**：断开→锁机/不锁机？接回→通过/不通过？等了多久锁机？
- ⚠ 注：**这块逻辑是 W7 前已验证的，实测用来印证。** 若行为与预期不符，是本次实测最有价值的发现之一。

## A5. 电源轨无误 + 继电器/LED 上电默认
- **方法**：上电后（未运行，未接主回路）用万用表测 ULN2003（继电器驱动）输出端 / 或直接听继电器的嗒声。
- **预期（待机默认）**：P0.22 RLY1 运行继电器**断开**、P0.21 RLY2 报警**断开**、（P0.20 RLY3 备用，看 reg61 使能态）；P1.20/21/22/23 状态 LED 按"未运行"态点亮（通常运行灯灭、故障灯灭）。
- **判定**：上电默认应**无运行继电器吸合**（安全）。若上电就吸合 → 危险，立即断电排查（可能锁机标志逻辑出错 / reg61 误置）。
- **记录**：RUN 继电器默认开/合、各 LED 亮灭。

**阶段 A 小结**：确认 MCU 活、电源好、认证正确、默认不输出。**全 PASS 才进 B。**

---

# 阶段 B：Modbus 通信验证（L1，只用 RS485，不动主回路）

> 目的：验证 MODBUS 从站收发 + 寄存器读写语义 + 与固件反汇编结论一致。**不接主回路也能测（此时 ADC 读的是 0 或无输入）。**

## B1. 连接 USB-RS485 到板 UART3
- 接线：USB-RS485 的 **A(B+)↔板 A**、**B(A-)↔板 B**；GND 共地（若板子是隔离 ADM2483，可先共地试，不行再全隔离）。
- 波特率/校验：先在**面板通讯菜单**设为与主机一致（常用 **9600,8,N,1**；地址先从 **1**）。注意通讯菜单：地址 `0x100016FF`、波特率索引 `0x10001700`→表 `0x100017BC`、校验 `0x10001704`。改完通讯参数**需重启生效**（反汇编确认）。

## B2. 用脚本/上位机读寄存器区
- 工具：minimalmodbus 脚本（§7 附录），或串口助手手动拼 Modbus 帧。
- **读 0x03（功能码），从 addr=1**，读 reg 40~45：
  - 预期 `reg40` = Ug 给定（面板给定值，未注入时=面板当前设定或 0）
  - `reg41/42/43` = IA/IB/IC 三相电流（未接主回路 → 接近 0）
  - `reg44` = IF 输出电流反馈（≈0）
  - `reg45` = Uf 输出电压反馈（≈0）
- **判定**：
  - ✅ 能批量读回且值合理（0 或面板设定值）→ 基本通信 OK
  - ❌ 读不到 / CRC 错 → **查**：地址是否对上、波特率校验是否一致、A/B 是否接反（对调试试）、RS485 隔离模块供电
- **记录**：读回 reg40-45 原始值（十进制）。

## B3. **关键验证：reg40 读写不对称（这是本项目最大陷阱）**
- 反汇编确证：`reg40` **读=实测（ADC 实际值）**，**写=远程给定源值 src_value**。
- 做法：写 `0x06` reg40 = 500 → 再读回 reg40。
- **预期**：读回**不是 500**（因为回读是实测值，未接主回路时≈0/面板值）。**这是符合预期的！** 若你"写 500 读回 500"说明这板子的 reg40 语义和你预想不同（记录）。
- **判定**：理解写→注入、读→实测的差异。**别把"写后回读不同"当 bug。**

## B4. reg61 远程输出使能 → 测 P0.20（**确立 P0.20 语义**——W8 待定结论之一）
- 反汇编：写 reg61 非 0 → 置 P0.20（BOM：P0.20 = RLY3 **备用继电器**）。
- 做法：0x06 写 reg61 = 1 → 万用表测 **P0.20**（或听 RLY3 是否吸合）。
- **预期**：P0.20 变为高 / RLY3 吸合。写 0 → 复位。
- **确定的问题**：reg61「远程输出使能」到底控制**备用继电器 RLY3** 还是**运行继电器 RLY1**（反汇编显示写 P0.20=RLY3，但语义可能是"远程使能整个输出"）。**这一步实测定论。** 记录 P0.20/RUN/ALM 三者哪个动。

## B5. reg24/25 读写不对称 + 0x06 vs 0x10 差异
- `reg24/25`：写→落「组4 增益槽」，读→「活动增益对」。写后再读**不应等于写过值**（预期不对称）。
- `0x10`（写多）vs `0x06`（写单）：0x10 无逐值范围校验、部分落 scratch `0x100017AC` 无实际效果、不触发参数同步。→ **改参数用 0x06**。实测验证 0x10 写后参数未真正生效。

## B6. 读取整段参数（可选，对照面板）
- 用 0x03 读段（如 reg 1~20 / 24~40 / 44~63），对照你在面板设的值。能对上说明 Modbus 与面板参数**同源**（反汇编确证 61 组 live↔EEPROM 同步）。

**阶段 B 小结**：通信通、reg40 读写不对称理解、reg61→P0.20 语义定论、0x06/0x10 差异确认。**B3/B4 是两个实测重点。**

---

# 阶段 C：MCU 触发引脚脉冲实测（L2，只测 ≤3.3V 引脚，**用信号发生器模拟过零**）

> 目的：**不接主回路**，用信号发生器给过零输入一个 50Hz 方波，骗 MCU"有电网过零"，从而让它按固件算法发出 SCR 触发脉冲。**这一步同时**给你看 SCR 触发逻辑 **G1-G6** 输出，全是低压引脚，安全。
> ⚠ 需要**先从 BOM/反汇编弄清哪根引脚是哪个触发通道**（见 §5 速查表），再用示波器去测。

## C1. 用信号发生器模拟过零（**必须！否则无触发脉冲**）
- **原理**：无过零 → MCU 认为无电网 → 不产生 SCR 触发序列（触发序列从过零中断 EINT 启动）。
- **做法**：信号发生器设为 **50Hz 方波，0→3.3V（或 0→5V，取决于板子类型）**，接到**同步过零输入**。反汇编：`EINT1=P2.11 (TV)、EINT2=P2.12 (TU)、EINT3=P2.13 (TW)`（⚠ 这项是交叉引用发现需实测定论的引脚——**以板子上你找到的那个引脚为准，或看原理图**）。**至少接一相过零**（如只接 TW/P2.13），就能看到对应相的触发。
- **判定**：接入后示波器应能测到 MCU 某触发引脚开始出现脉冲。

## C2. 抓 G1-G6 触发脉冲（核心）
- **做法**（示波器，10:1 探头，各自对 GND）：依次测 6 个触发引脚。**看一相**（比如 G1= P0.17）。
- **预期波形**（以 50Hz、起始相位=0 为例）：
  - 每 **20ms**（50Hz 周期）一个**窄脉冲**；
  - 脉冲宽度 = **触发脉冲宽度，约 12°** = **667μs**（若固件是 12° 脉宽、120° 或 180° 导通方式会看到"每周期两个脉冲"）。
  - **6 组触发（G1-G6）之间的相位差为 60°** = **3.333ms**（即 G1→G2 差 3.333ms，G2→G3 又差 3.333ms…，G1 到 G4 差 10ms=180°，G1 到 G7=循环）。
  - 若看到 **60° 双脉冲**（在一个 180° 导电窗口内，两个间距约 60° 的窄脉冲），那就是"60° 双脉冲触发方式"——两种都算正常，取决于固件设定，**记录看到的是哪一种**。
- **判定**：✅ 看到 ~667μs 窄脉冲、6 组相位相隔 ~3.333ms → 触发逻辑正确；❌ 无脉冲 → C1 没接对 / 相位限制等参数设成了 0 / 未进入运行态（C3）。
- **记录**：抓 G1 的波形截图（或写周期/脉宽/相邻组间隔）、6 组是否都出现。

## C3. 需要先"运行"才触发（启动方式）
- **原理**：很多触发只发生在"运行"状态（P0.22 RUN 继电器吸合、运行 LED 亮）。可能要先用面板 RUN 键、或远程 reg61 使能，才会出脉冲。
- 操作：进入运行态（面板确认运行灯亮 / 继电器嗒一下）再看 C2。
- **预期**：进入运行态后才看到触发脉冲。

## C4. 验证触发角随参数变化（关键——移相角命令）
- **原理**：输出路径 `输出 = 180° − 当前角`（应用指南：起始相位=输出下限，触发角 = 180 − 当前角）。改变「起始相位」参数 = 改变输出（移相角）。**相位限制（reg9）** = 上限。
- 做法：在面板把**起始相位**从 0 改到 **30** → 再次抓 G1 波形。
- **预期**：触发脉冲**相对过零点的相位位置**随之移动（触发角=180−30=150°，即**过零后 150° ≈ 8.333ms** 处才触发）。起始相位大 → 触发角小 → 导通占比变大。
- **记录**：起始相位=0 和 =30 两档时，G1 触发相对过零点的延时（ms）。

## C5. 软起动序列（phase 0→4→5）
- 反汇编：软起动 phase `0→4→5`（0=停，4=运行，5=稳定）。启动时触发角从大（接近 180°）逐渐减小到目标 → 输出逐渐上升。
- 做法：触发关键点（RUN）瞬间抓 G1，用**单次/滚屏**示波器模式看起动脉冲相位**逐渐前移**（从靠近 180° 移到目标角）。初段会看到触发角逐渐变小。
- **记录**：能否看到软起渐升过程，观察软起时间≈你设的屏 7 参数。

## C6. 极性确认：置高=触发还是封锁（反汇编待定项）
- gpio_outputs_set 里**置高到底是"触发"还是"封锁"**——需实测。从波形看：触发引脚在**有触发时高电平窄脉冲** = 置高触发；如果想翻转（平时高、触发时拉低）就是封锁极性。
- **记录**：平时电平（高/低）、触发时脉冲极性。

**阶段 C 小结**：验证了触发脉冲的**宽度、相位、间隔、移相能力、软起**。若全对，控制逻辑基本吻合反汇编。

---

# 阶段 D：强电闭环（L3，必须持证电工，380V/220V，最高风险）

> **这一阶段要接真实三相负载、用强电。我只给流程参考，操作必须由电工按安全规程来。** 强烈建议先调最保守参数（见准备清单）。

## D1. 接负载（白炽灯/小电机）+ 接线
- 三相进线接晶闸管主回路输入，负载（如 3 个 220V 白炽灯串在三相，或小三相电机）接输出侧。
- 用**差分探头**或**隔离示波器通道**测晶闸管输出（严禁普通探头）。

## D2. 上强电，逐渐升给定
- 控制方式设为…面板手动升给定 → 用示波器抓**输出侧波形**（移相控制下的「阶梯状」正弦残缺波——每个半周期有一个**缺口**，缺口宽度=触发角）。
- **读取移相角**：输出波形从正/负半周上升沿到**实际导通点**之间的缺口角度 = 触发角。它应随给定增大而**减小**（输出升高）。
- **记录**：比如给定=0 时触发角≈180°（几乎无输出）、给定增大触发角减小。

## D3. ADC 标定核对（reg44/45 + reg40-43）
- **做法**：同时记录（a）Modbus 读回 reg40-45 原始值；（b）万用表（真有效值）实际测的负载电压/电流。
- **得结论**：对每个通道算**比例系数** = 实际工程值 / Modbus 原始值。反汇编已知 5 个标定除数在 regs 51-55（值 3500~4500），把实测系数与这些值对照，验证工程单位标定。
- **记录表格**：`通道 | Modbus 原始值 | 万用表实测 | 比值 | 标定除数(reg51-55) | 匹配?`
- **预期**：比例系数应与固件标定除数一致（或能解释）。

## D4. 保护功能验证（刻意降阈值，最后做）
- **务必最后做，且用刻意设低的阈值**来触发，别用生产设定触发（会误动作/危险）。
- 想触发哪类保护，就把该类阈值调**低于**当前实际值：
  - **过压**：设过压阈值 < 当前输出电压 → 触发。
  - **过流**：负载加重 → 电流超阈值。
  - **缺相**：断开其中一相进线 → 缺相判据。
  - **三相平衡**：设平衡百分比很小（如 2%）→ 正常三相也判不平衡（**仅做功能确认用，测完恢复**）。
- **触发后预期**：P0.22 运行继电器 RLY1 释放（停机）、P0.21 RLY2 报警继电器吸合、P1.23 故障 LED 亮、输出停止、**锁机标志置位（0x1000EDF4 只置不清，需断电复位）**。
- **记录**：每类用哪种方法触发、是否触发、触发后哪几个继电器/LED 动作。

## D5. 停机斜坡 / 软停
- 触发停机（面板 STOP / 强停）：观察输出是否**斜坡下降**（软停，非立即切断）——反汇编有"停机斜坡"逻辑。看触发角**逐渐增大**到 180°（输出渐降到 0）。
- **记录**：软停时间≈进度设置值？是否为斜坡。

**阶段 D 小结**：验证移相输出、ADC 标定、保护链、停机斜坡。**这是整套逆向的最终闭环。**
**如果 D 阶段全 PASS**，说明反汇编还原代码与真机行为一致 → 目标 B 的"等价固件"立起来了。

---

# 5. 关键引脚 / 预期值速查表（实测时对照）

| 项 | 值 | 来源 |
|---|---|---|
| 触发 GPIO G1-G6 | P0.17 / P0.15 / P0.18 / P2.9 / P0.19 / P0.16 | 反汇编+BOM（W 已确证） |
| 12 脉波 | P2.8/7/6/5、P0.8/7 | 反汇编 |
| 同步过零 EINT1/2/3 | P2.11/12/13（⚠ 交叉引用待实测确认） | 反汇编⚠ |
| 认证 1-Wire | P2.1 出 / P2.2 入 / P2.3 时钟 / P2.4 复位 → ADuM1201 | BOM 确证 |
| LCD | 12864：P1.25/26=CS、P1.27=RS、P1.28=E、P1.x=DB0-7 | 反汇编⚠ |
| 继电器 | P0.22=RLY1 运行 / P0.21=RLY2 报警 / P0.20=RLY3 备用 | BOM（reg61→P0.20 待实测） |
| 电网周期 | 50Hz：20ms；60Hz：16.667ms | 数学 |
| 电角度 1° | 50Hz：55.56μs；60Hz：46.3μs | 数学 |
| 触发脉宽 12° | 50Hz：约 667μs；60Hz：约 556μs | 数学+固件 |
| 60° 间隔 | 50Hz：3.333ms；60Hz：2.778ms | 数学+固件 |
| 触发角=180−待测角 | → 触发相对过零延时 | 应用指南 |
| reg40-45 | Ug/IA/IB/IC/IF/Uf 供标定 | 反汇编 |

> ⚠ 凡我标「⚠」的（EINT 引脚、LCD CS/RS/E、继电器编号、reg61 语义）都是本项目**已知存在差异/待实测确认**的交叉引用点——实测时**以板子实物/原理图为准**，这些正是 W8 要定论的。

---

# 6. 我怎么介入（AI 帮你）

我**不能碰你的硬件**，但我能在这些环节帮你：

1. **生成预期值/判 PASS**：你把任一阶段的实测读数（或示波器截图、波形参数）贴回来，我对照上面的预期值判定，告诉你 PASS/FAIL + 可能原因 + 下一步。
2. **生成 Modbus 测试脚本**：§7 附录有 minimalmodbus 全套脚本（读 reg40-45、写 reg40/61、验证不对称）。你只要改一下 COM 口，直接跑。
3. **生成"标定换算"表格**：你把 D3 的原始值+万用表值贴我，我帮你算每个通道比例系数，对照固件标定除数，判断标定是否对。
4. **分析示波器波形**：你的示波器能存 CSV/截图的话，保存后贴图给我（或描述波形参数：脉宽、周期、相邻组间隔），我核对是否 = 12°/60°/3.333ms。
5. **定位 FAIL**：任何一步结果跟预期不同，把**结果和现象**给我，我结合反汇编逻辑反推可能是哪个参数/哪个引脚/哪个符号偏差导致（这正是静态分析最擅长的）。

### 结果回填模板（复制到记事本，边测边填，最后贴回）
```
[阶段X 步骤Y]
- 引脚/位置：____
- 工具/挡位：____
- 实测读数：____
- 预期值：____
- 判定(PASS/FAIL)：____
- 现象/截图描述：____
- 不确定的地方：____
```

---

# 附录：minimalmodbus 测试脚本（阶段 B 用）

> 需先 `pip install minimalmodbus pyserial`。把 `PORT` 换成你的 USB-RS485 串口号（Windows 看设备管理器 COMx）。默认 9600,8,N,1，从站地址 1。

```python
import minimalmodbus, time

PORT  = "COM5"     # 改成你的串口号
ADDR  = 1          # 从站地址（面板通讯菜单地址）
BAUD  = 9600
inst  = minimalmodbus.Instrument(PORT, ADDR)

inst.serial.baudrate = BAUD
inst.serial.bytesize = 8
inst.serial.parity   = 'N'
inst.serial.stopbits = 1
inst.serial.timeout  = 1

def reg_read(reg, n=1, dec=0):
    return inst.read_register(reg, dec, 3)      # 0x03 读

def reg_write(reg, val, dec=0):
    return inst.write_register(reg, val, dec)   # 0x06 写单

print("== 读 reg40-45 ==  (未接主回路应近乎 0/面板值)")
for r in range(40, 46):
    try:
        print(f"  reg{r} = {reg_read(r)}")
    except Exception as e:
        print(f"  reg{r} 读失败: {e}")

print("== reg40 读写不对称测试 ==  写 500 再回读")
try:
    reg_write(40, 500)
    print(f"  写 reg40=500 后回读 = {reg_read(40)}  (预期≠500)")
except Exception as e:
    print(f"  失败: {e}")

print("== reg61 远程输出使能 = 1 (测 P0.20/RLY3) ==")
try:
    reg_write(61, 1); print("  已写 reg61=1，请用万用表测 P0.20")
    time.sleep(1)
    reg_write(61, 0); print("  已写 reg61=0")
except Exception as e:
    print(f"  失败: {e}")
```

> 运行报 `CRC`/`No response` → 先按 B1 排查（地址/波特率/A-B 反接/共地）。

---

## 参考
- 反汇编模块：`01_startup`（主循环）、`03_input_debounce`、`05_adc`、`07_state_machine`、`08_uart3_modbus`/`08_modbus_dispatch`、`09_output_stage`、`11_auth`、`13_gpio_init`。
- 文档：`APPLICATION_GUIDE_2026-08-21.md` §八、`WORK_GUIDE_2026-08-21.md` W8、`docs/HARDWARE_VERIFICATION_2026-08-20.md`、`docs/MENU_PARAMETER_MAPPING.md`、`firmware/README.md`。
