# 拿到硬件后的开测总入口（START HERE）

> 日期：2026-08-22 ｜ 谁用：**拿到板子和仪器、准备上真机的人**（可能是在另一台电脑上操作）
> 唯一用途：把「从拿到东西 → 到测完 W8」串成一条路线，**按顺序照走即可**。
> 遇到每一步点开对应文档；把数据贴回 AI（见我下面「☁️ AI 协作」）。

---

## ⚠️ 开门三件事（先读）

1. **安全**:这块板控制**三相强电（380V）**。下面"阶段 D"必须持证电工+差分/隔离探头。**普通探头测高压会烧示波器。**
2. **这 5 点只有实测能定论**（静态推不出来）：EINT 过零引脚、LCD CS/RS/E 引脚、继电器编号、**reg61 控制哪个继电器**、**触发置高=触发还是封锁**。测的时候专门核对这些。
3. **你的目标**：验证我们反编译出的固件 `firmware.hex` 在真机上跑的**行为**是否与原始 PC6M-10 一致。

---

## 路线总览（4 条腿）

```
① 备齐东西  →  ② 搭好环境  →  ③ 能编译/烧录  →  ④ W8 三档实测  →  ☁️ 交给 AI 判读
```

每腿「做什么 / 打开哪个文档 / 成功标志」如下，按序走。

---

## ① 备齐东西（清单）

**硬件**（√ 对着勾）：控制板(PC6M-10 或新板)、示波器(≥2通道)、**差分/隔离探头**、信号发生器、USB-RS485 转换器、万用表、接线/鳄鱼夹。电源（控制板供电 + 三相强电，由电工接）。

**待你用前确认**：这块板的 **烧录方式/烧录器**（LPC1765 常用 **SWD(J-Link/CMSIS-DAP)** 或 **UART ISP**，取决于板子引出的编程口）。见 ③ 。

---

## ② 搭好环境（另一台电脑）

打开（或双击）：
- **`tools/setup_windows.bat`** —— 一键装 Git+ARM GCC+Python + minimalmodbus/pyserial

说明文档：**`SETUP_WINDOWS.md`**。脚本会自动修正 `build.sh` 的硬编码路径。

**✔ 成功标志**：能跑 `bash build.sh` 出 `OK: firmware.elf/.hex/.bin`。

---

## ③ 能编译 / 烧录

- 编译：`cd firmware` → `bash build.sh` → 出 `firmware.hex / firmware.bin`。
- **烧录**：把 `firmware.hex` 烧进 LPC1765。方法取决于你的烧录器：
  - **SWD**（J-Link / CMSIS-DAP / DAPLink）：用配套软件（J-Flash / OpenOCD）烧 hex。
  - **UART ISP**：连接 P0.1/P0.2 到串口，用 Flash Magic 等烧 hex（需先正确设置 P2.9/P2.10 进入 ISP）。
  - ⚠ 具体引脚/命令按你手上的板子和烧录器——**这一步需要你确认烧录器型号后我再帮你写具体的烧录命令**。

**✔ 成功标志**：上电后（只给控制电）LCD 亮、能进版本菜单（见 W8 清单阶段 A）。用了我们编译的固件，就是"复刻固件"在跑；测试行为是否与原始板一致，是 W8 目的。

---

## ④ W8 三档实测（从最安全开始，逐步升级）

> 主清单：**`W8_HARDWARE_TEST_2026-08-22.md`**（每步的接线/预期值/判定/记录字段）
> 软件操作：**`W8_SOFTWARE_OPERATION.md`**（示波器挡位/信号发生器/串口脚本怎么用）

### L1 只读（安全，独自可做）
- **A 上电自检**：LCD 亮、3.3V/5V、晶振、认证(断开/接回隔离模块)、继电器默认。
- **B Modbus**：`w8_serial_detect.py` 认 COM 口 → `w8_modbus_test.py` 读 reg40-45、验 reg40 读写不对称、写 reg61。

### L2 低压（较安全）
- **C 触发脉冲**：信号发生器设 **50Hz 方波 0-3.3V** 接到过零输入 → 示波器测 G1-G6，抓 **12° 脉宽 / 60° 间隔**。可存 CSV 用 `w8_analyze_wave.py` 自动判。

### L3 强电（必须持证电工）
- **D 移相输出**：真实负载，差分探头抓输出残缺波，读**触发角 = 180−给定**趋势；**ADC 标定**（reg40-45 原始值 vs 万用表）；**保护链**（刻意降阈值触发）；**停机斜坡**。

**✔ 每条 PASS** → 逐步逼近"复刻固件行为等价"。

---

## ☁️ 喂给 AI 判读（我帮你判 PASS/FAIL + 定位）

把下面任一种**原样贴回**（对话里发我）即可，我会对照预期值给结论：

| 你拿到 | 给我 | 我回你 |
|---|---|---|
| Modbus 读数 | `w8_modbus_test.py` 输出 | reg 值是否合理 + PASS/FAIL |
| 波形 | 截图 / `w8_analyze_wave.py` 输出 | 是否=12°/60°/3.333ms，定位差异 |
| 标定数据 | Modbus 原始值 + 万用表实测 | 比例系数，对照标定除数 reg51-55 |
| 任何 FAIL | 现象 + 读数 | 结合反汇编逻辑反推原因 |

> **逐条记录用这个模板**（复制到记事本，边测边填，最后整段给我）：
> ```
> [阶段X 步骤Y] 引脚/位置：__ 工具/挡位：__ 实测：__ 预期：__ 判定：__ 现象：__ 不确定：__
> ```

---

## 需要你确认 / 我还能帮你做

- **烧录器型号**：告诉我你用 J-Link / CMSIS-DAP / 串口 ISP 哪个，我帮你写具体烧录命令。
- **EINT/LCD/继电器引脚**：你手上板子/原理图上的实际引脚若与我表里不同，发我，我更新结论。
- 任何一步结果异常 → 把现象发我，我定位是参数、引脚还是符号问题。

---

### 快速跳转
| 要什么 | 去哪 |
|---|---|
| 装软件 | `tools/setup_windows.bat` / `SETUP_WINDOWS.md` |
| 编译固件 | `firmware/` → `bash build.sh` |
| 上电自检+Modbus | `W8_HARDWARE_TEST_2026-08-22.md` 阶段 A/B |
| 抓触发脉冲 | 阶段 C + `W8_SOFTWARE_OPERATION.md` §4 |
| 强电移相+标定 | 阶段 D（持证电工） |
| 脚本 | `tools/w8_serial_detect.py` / `w8_modbus_test.py` / `w8_analyze_wave.py` |
