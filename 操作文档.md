# 傻瓜式操作文档：构建与烧写

> 三个双击即用的 Windows 脚本，覆盖「装依赖 → 构建固件 → 烧写固件」全流程。
> 面向非开发者的傻瓜式操作：**全部双击运行，按提示操作即可**。

## 概览

| 脚本 | 作用 | 用法 |
|---|---|---|
| `install_deps.bat` | 一键安装构建/烧写依赖（首次运行） | 双击，自动请求管理员 |
| `build.bat` | 编译最新固件 | 双击 |
| `flash.bat` | J-Link SWD 烧写固件到板子 | 双击；或用 `-check` 先测连接 |

## 硬件前置（烧写前必读）

- 断开**市电 / 门极 / 功率负载**，板子仅接**控制电**（安全要求，见 W8 实测规范）。
- 板子接好 J-Link，**P12 排针**线序：
  | P12 脚 | 信号 |
  |---|---|
  | 1 | VTref |
  | 2 | GND |
  | 6 | SWDIO |
  | 7 | nRESET |
  | 8 | SWCLK |

## 一、安装依赖 `install_deps.bat`

首次在新电脑上使用时运行。会自动请求管理员权限，依次检查并安装：

1. **Git for Windows**（`build.bat` 需要 bash）—— 缺则提示到 git-scm.com 下载；
2. **Arm GNU Toolchain 14.2.Rel1**（编译工具链）—— 缺则自动下载官方安装包并**静默安装**（约 250MB 下载，装到标准路径）；
3. **J-Link USB 驱动**（仓库自带 `tools\jlink` 打包驱动）—— 用 J-Link 实连探测，识别不到才自动装驱动，装完**自动复测**确认驱动生效。

可选参数（双击时加在文件后面，如用命令行）：
- `-skip-toolchain` —— 跳过工具链检查/安装
- `-skip-driver` —— 跳过 J-Link 驱动检查/安装

完成后提示：`build.bat` 构建、`flash.bat` 烧写。

## 二、构建固件 `build.bat`

双击运行，自动：
1. 定位 Git Bash（已装 Git for Windows 即可）；
2. 自动探测 Arm GNU Toolchain（任何版本，常见安装目录 + PATH；缺则提示先运行 `install_deps.bat`）；
3. 编译固件，产物输出到 `firmware\`：
   - `firmware.elf` / `firmware.hex` / `firmware.bin` / `firmware.map`
4. 构建完整输出落盘 `backup\build.log`，显示**构建耗时**与 `firmware.bin` 的 SHA-256（与 `AGENTS.md` 记录的当前版本一致）。

构建失败会停在错误码并显示日志 `backup\build.log`。成功后可运行 `python test\run_tests.py` 做全量回归测试。

## 三、烧写固件 `flash.bat`

双击运行，自动执行安全烧写流程：

1. **连接探测** —— 先用 J-Link 实连目标，找到 `SW-DP` 并显示芯片 ID（`0x2BA01477` = Cortex-M3），找不到（驱动/接线问题）立即中止并提示；
2. **烧写确认** —— 显示待烧固件的文件名 / 构建时间 / 大小 + 自动备份路径，按 `Y` 确认后才继续（整片擦除不可逆，防误烧旧版）；
3. **connect-under-reset** —— 复位引脚控制下进入调试（固件复用 SWD 脚也能连上）；
4. **自动备份** —— 把板上当前 Flash 读到 `backup\auto_pre_flash.bin`；
5. **整片擦除** → **写入 `firmware\firmware.bin`** → **全镜像校验**；
6. **复位运行** 新固件。

### 可选参数

| 参数 | 作用 |
|---|---|
| `-check` | 只做 J-Link 连接探测（校验驱动/接线）并显示芯片 ID，**不烧写**。想先确认能不能连上时用 |
| `-preview` | 只生成 J-Link 命令脚本并显示，**不执行**。想看烧写命令序列时用 |
| `-norun` | 烧写+校验完成后**不复位运行**（内核保持 halt），排查时用 |

### 失败排查

烧写失败会显示日志 `backup\flash_build.log`，常见原因：
- 提示找不到 SW-DP → 驱动未装（先跑 `install_deps.bat`）/ J-Link 未插 / 板未供电 / **P12 线序错**；
- 擦除/写入报错 → 供电不稳、接线松、J-Link 线长或干扰；
- 校验失败 → 先做 `-check` 排除连接问题，确认线序与供电。

## 常见问题

**Q: 工具链要装到哪？** 已改为**自动探测**：`build.bat` 与 `firmware\build.sh` 按「约定版本 → 常见安装目录最新版 → PATH」自动找工具链，任意版本都能构建。默认装到 `C:\Program Files (x86)\Arm GNU Toolchain arm-none-eabi\14.2 rel1`（`install_deps.bat` 自动装到此路径）。

**Q: J-Link 软件要安装吗？** 不用。仓库已打包免安装最小集，统一从 **`tools\jlink\JLink.exe`** 调用（`flash.bat`、`install_deps.bat` 优先用它，其次才自动探测本机安装版 / PATH）。**后续所有 AI 工具与脚本一律调用该打包路径**，禁止依赖本机其他安装位置。驱动缺失时才需要装（`install_deps.bat` 自动处理）。

**Q: 脚本里写死的路径换了电脑怎么办？** 不用手改，三处硬编码已全部改为自动探测：
- **J-Link** → `tools\jlink` 打包版优先；其次自动探测 `%ProgramFiles%` / `%ProgramFiles(x86)%\SEGGER\JLink_V*`；最后 `PATH`。
- **工具链** → `build.bat` / `build.sh` 自动探测任意版本（约定版本优先 → 常见安装目录最新版 → PATH）。

**Q: 工具链很大，能一起打包吗？** 不能。Arm GNU Toolchain 解压后约 **1.2GB**，远超仓库打包范围，故用 `install_deps.bat` 自动下载静默安装。

**Q: 烧写会自动备份原固件吗？** 会。每次烧写前把当前 Flash 读到 `backup\auto_pre_flash.bin`（256KB），如需还原可用该备份配合 J-Link 写回。
