# LPC1765FBD100 固件逆向工程 — 项目全史（开始 · 过程 · 结果）

> 整理日期：2026-08-21
> 本文档是本次逆向工作的**总纲**：起因 → 过程 → 结果 → 参考索引。
> 细节文档见 `docs/` 子目录，反编译源码见根目录 13 个 `.c` 模块。

---

## 一、项目开始

| 项 | 值 |
|---|---|
| 固件 | `LPC1765.bin`（NXP **LPC1765** / Cortex-M3，256KB flash @0x00000000，262144B） |
| 设备 | **PC6M-10 三相晶闸管（SCR）移相触发功率控制板**（SINE POWER / ST33C 变频电源；恒压/恒流/开环三模式） |
| 目标 | ① 完全理解固件功能 ② 反编译全部函数 ③ 长期目标：还原为可编译、可修改的等价源码 |
| 工具 | Ghidra 12.1.3 + Ghidra MCP（本地服务，读取超时 5s、单线程）、Python 分析脚本、用户提供的 `doc/` 硬件文档（原理图/BOM/U38 接线表/分析报告/面板手册） |
| 环境 | Windows 11 / Git Bash；Ghidra 工程 `D:\code\LPC1765FBD100\LPC1765FBD100.gpr` |

起点：对设备固件二进制进行静态逆向。会话曾因死机丢失，后用 `PROGRESS_2026-08-20.md`（防丢失主文档）恢复。

---

## 二、过程（阶段时间线）

### 阶段 0：工具准备与工程建立
- 建立 Ghidra 工程、导入 bin、补 SRAM 段。
- 编写辅助脚本（现迁入 `tools/`）：`AddSramAndVars.java`（补 SRAM 段与变量标号）、`CreateIsrFunctions.java`（创建 ISR 函数体）、`AnalyzeStateMachine.java`（状态机流程导出）、`TypeLiteralPointers.java`（字面量指针类型化）、`create_isr_functions.py`。
- 早期专题分析（现迁入 `docs/`）：`state_machine_analysis.md`（状态机拆解）、`uart3_protocol.md`（Modbus-RTU 协议）、`i2c_param_sync.md`（I2C 参数系统）。

### 阶段 1：核心架构确证（2026-08-20）
- **63 个 Modbus 寄存器**全量解析（范围校验、读写不对称、别名）。
- **61 组参数 live↔shadow↔EEPROM** 同步映射全表（`FUN_000035f2`）。
- **ADC0 6 通道测量链路**：5 点移动平均 + 标定除数，产出 reg40-45。
- **闭环链路**：`FUN_00010f0a` → `FUN_000108b0`（误差分区选增益 + 积分滤波）。
- **输出级链路** `FUN_0000e9ac`：软起动 phase 0→4→5 + 闭环 + 报警积分 + 限幅 + 停机链。
- **重大发现**：LPC1765 无本地 PWM/DAC，但用 **TIMER2(0x40090000) 编程 SCR 触发角**——EINT3 每个输入过零算 `MR0 = out_freq_adj×10 + BASE + out_fine×K − out_scale`；TIMER1 扫描 240 步生成 6 窗口触发脉冲（60° 双脉冲 / 载波 / 窗口边界 1160μs）。推翻"无输出外设"旧结论。

### 阶段 2：硬件印证（2026-08-20）
- 用户提供 `doc/` 硬件文档 → 设备定位**三相晶闸管移相触发板**。
- **芯片清单确认（依据 doc/BOM 表 `PC6M-10-BOM-更新版.xlsx`，HARDWARE_VERIFICATION §六）**：AT24C02C（EEPROM）、LPC1765、HEF40106×2、FR120N×6、ULN2003A、ADuM1201（认证隔离）、KMB419-301S×6（主桥）、继电器×3、RS8552XM 运放、LM2904×3、LM2901、CJ431、PE5420→KBP310/MB6S→LM2575S-5.0→AZ1117H-3.3 电源树、1W 0.1R×3 采样。
- **触发 GPIO**：G1-G6 = P0.17/15/18/P2.9/19/16 + 12 脉波 P2.8/7/6/5/P0.8/7（12° 脉宽 / 60° 双脉冲）。
- **认证链路**：P2.1-2.4 经 **ADuM1201** 隔离 1-Wire 挑战-应答（防克隆，失败锁机）。
- 显示 = **12864 图形 LCD**（P1 口）；UART3 = RS485（ADM2483 隔离模块 U5）。

### 阶段 3：菜单映射（2026-08-20，任务#16）
- 面板菜单树→固件参数**全量确证**：基本参数 **16 屏**（手册仅 12）、保护 10 屏、通讯 4 屏、PID 9 屏、相位校准、恢复出厂（密码门控）、版本信息。
- **旧标修正**：gain_a/b = 电压/电流量程、out_fine = 主从偏移、sub_state = 控制方式、reg12 = 启动方式、reg13-20 = 保护参数、缺相/三相平衡地址、reg62 = 起始相位。→ `MENU_PARAMETER_MAPPING.md`

### 阶段 4：全函数反编译导出（2026-08-20 晚 ~ 08-21）
- Ghidra `list_functions` 确认 **105 个函数**。
- **103 个中小函数**逐一 `decompile_function` → C 反编译，写入 13 个 `.c` 模块。
- **2 个超大函数**（`state_machine` 0x458C、`modbus_dispatch` 0xB642）C 反编译结果超 MCP 5s 传输上限、连续超时 → **改走全量反汇编落盘 + 人工精读还原**（`disassemble_function` → `.txt` 附录 10061/5161 条指令，另在 `.c` 中给出流程还原伪代码 + 关键代码段注释）。
- 每函数标注真实入口地址 `/* 0x0000XXXX —— 名称 */`，保留 Ghidra 符号。

### 阶段 5：评估与工作指导（2026-08-21）
- 回答：反编译流程是否走完 / 代码是否完整 / 能否重新编译 / 未完成项 / 优化方向 / 建议。
- 结论写入 `WORK_GUIDE_2026-08-21.md`（W1-W8 工作清单 + 目标 A/B 路线）。

### 阶段 6：目录整理与项目档案（2026-08-21）
- 原始 `LPC1765.bin` 迁入本目录。
- 根目录关键信息/文件/流程迁移或总结进 `docs/`、`tools/`。
- 撰写本全史 + `CLAUDE.md`（自包含项目索引，任何 AI 可快速接手）。

### 阶段 7：目标 A 收尾（2026-08-21，三项实施）
- **② 数据段清单导出** → `DATA_SEGMENT_2026-08-21.md`（内存布局/向量表/字符串池 GBK 全表/查表+反编译代码/关键魔数/UART3 指针表 0xB00C-0xB094 全映射/SRAM 全局清单），同时是目标 B 的 W2 地基。
- **③ 交叉引用**：13 个 .c 模块头注加"交叉引用"段指向 docs；发现 3 处引脚/编号差异（EINT1/2/3 实为 P2.11/12/13、LCD CS/RS/E、继电器编号）已 ⚠ 标注。
- **① 两大函数注释补完**：`state_machine` 补事件码→菜单页分发链；`modbus_dispatch` 补 51 写分支结构。
- 按指示"此次实施完成之后根据最新结果更新所有文档"——CLAUDE/SUMMARY/README/WORK_GUIDE/记忆全部同步。

---

## 三、结果

### 3.1 成果清单
| 类别 | 产出 |
|---|---|
| 函数覆盖 | **105 个函数全部处理**，机器指令无丢失 |
| C 反编译 | 103 个 → 13 个 `.c` 模块（启动/LCD/按键/I2C/ADC/参数/状态机/Modbus/输出级/继电器/认证/闭环/GPIO） |
| 反汇编附录 | `state_machine`（10061 条）、`modbus_dispatch`（5161 条）全量 `.txt` |
| 协议/架构 | Modbus-RTU（reg 1..63）、61 组参数同步、I2C@0x53=AT24C02C、1-Wire 认证、TIMER2 SCR 触发时序 |
| 映射文档 | 菜单→参数、硬件印证、寄存器表、芯片同步表 |
| 目标A 收尾 | `DATA_SEGMENT_2026-08-21.md` 数据段清单 + 13 模块交叉引用 + 两大函数注释补完（2026-08-21） |
| 理解度 | **主控制路径 100% 理解，整体 ~95%** |

### 3.2 关键结论速查（详情见各文档）
1. **Modbus RTU 从站**：UART3（RS485），功能码 0x03/0x06/0x10，CRC16 poly 0xA001（双 256 表），精确地址匹配（0x1000B988）。
2. **EEPROM @0x53** = AT24C02C（256B 被动参数存储）；I2C 为 GPIO 位带模拟（SDA=P0.10、SCL=P0.11）。
3. **认证**：1-Wire 挑战-应答（24 位挑战/16 位应答，P2.1 出/P2.2 入/P2.3 时钟/P2.4 复位，经 ADuM1201），失败重试 5 次后锁机。
4. **SCR 触发**：EINT1/2/3（P2.11/12/13 过零）→ TIMER2.MR0 定触发角 → TIMER1 扫描 6 窗口×40 步生成 G1-G6 + 12 脉波触发脉冲。
5. **闭环**：位置式积分 + 误差分区选增益 + 钳位，三路（第一路/第二路/恒压源）。
6. **软起动**：phase 0→4→5，门控 = cfg_word==1 && reg40 读回源 ≥10。
7. **保护**：0x1000EDF4 位标志（bit4 过压 / bit5 过流 / bit3 缺相 / bit9=缺相严重级；只置不清锁存），停机斜坡。
8. **菜单状态机**：14 位状态标志 → 事件码 1..0xE；运行中每 120s/300s 触发参数 EEPROM 同步。

### 3.3 当前局限（不可直接编译）
1. 数据段清单已导出（`DATA_SEGMENT_2026-08-21.md`），但 RAM 初始镜像值、位域定义未落地。
2. 120+ 个 `DAT_0000xxxx` 全局符号无声明。
3. 无外设寄存器头文件（CMSIS/LPC176x）与 IAR 工程（.ewp/.icf/启动文件）。
4. 两大函数为伪代码级还原，非可编译 C。
5. Ghidra 反编译丢失 volatile/访问宽度/对齐语义，行为等价无静态保障。

→ 完整未完成清单见 `WORK_GUIDE_2026-08-21.md`（W1-W8）。

---

## 四、参考文档索引

| 文档 | 说明 |
|---|---|
| `CLAUDE.md` | **项目自包含索引（入口）**，任何 AI 接手先读此文件 |
| `README.md` | 反编译导出说明：模块布局 / 内存映射速查 / 功能结论 |
| `PROJECT_SUMMARY_2026-08-21.md` | 本文档（全史） |
| `APPLICATION_GUIDE_2026-08-21.md` | 应用指南：逆向结论怎么用（Modbus/菜单对照/故障诊断/硬件更换/行为定制/实测清单） |
| `DATA_SEGMENT_2026-08-21.md` | 数据段清单：内存布局/向量表/字符串池 GBK 全表/查表/关键魔数/UART3 指针表/SRAM 全局（目标A②，W2 地基） |
| `WORK_GUIDE_2026-08-21.md` | 工作指导：评估 / W1-W8 / 目标 A/B 路线 |
| `docs/PROGRESS_2026-08-20.md` | 防丢失主文档：63 寄存器表、61 组同步全图、方法论 |
| `docs/PLAN.md` | 进度与计划、关键符号速查表 |
| `docs/HARDWARE_VERIFICATION_2026-08-20.md` | 硬件印证（BOM 芯片清单/触发 GPIO/12864 LCD/认证链路） |
| `docs/MENU_PARAMETER_MAPPING.md` | 菜单树→参数全量映射 |
| `docs/i2c_param_sync.md` | I2C 参数系统（live/shadow/双银行/寄存器映射） |
| `docs/uart3_protocol.md` | Modbus-RTU 协议逆向 |
| `docs/state_machine_analysis.md` | 状态机拆解 |
| `docs/*_disasm.txt` / `docs/*_flow.txt` | 早期指令级转储（历史中间产物） |
| `docs/doc/` | **doc/ 硬件文档原件**（BOM/原理图/U38 接线表/面板手册/连接表/提取文本，8 个文件） |
| `tools/*.java` / `tools/*.py` | Ghidra 辅助脚本 |
| `01_startup.c` … `13_gpio_init.c` | 反编译源码（13 模块，函数入口地址标注在注释） |
| `07_state_machine_asm.txt` / `08_modbus_dispatch_asm.txt` | 两大函数全量反汇编附录 |
